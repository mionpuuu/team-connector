const attendanceSection = document.querySelector('#attendance-section');
if (attendanceSection) {
  attendanceSection.innerHTML = "<%= j render partial: 'events/attendance_list', locals: { event: @event } %>";
}
alert('不参加として登録しました。');