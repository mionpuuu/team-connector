<div class="mt-8 border-t pt-4">
  <h3 class="text-lg font-bold mb-2 text-gray-800">参加状況</h3>

  <% if event.attendances.any? %>
    <ul class="space-y-1">
      <% event.attendances.includes(:user).each do |attendance| %>
        <li>
          <span class="font-medium"><%= attendance.user.name %>：</span>
          <% case attendance.status
             when "attending" %>
              <span class="text-green-600 font-semibold">◯</span>
             <% when "absent" %>
              <span class="text-red-600 font-semibold">×</span>
             <% when "pending" %>
              <span class="text-yellow-600 font-semibold">△</span>
          <% end %>
          <% if attendance.notice.present? %>
            <span class="text-gray-600 text-sm ml-2">（<%= attendance.notice %>）</span>
          <% end %>
        </li>
      <% end %>
    </ul>
  <% else %>
    <p class="text-gray-500">まだ回答がありません。</p>
  <% end %>
</div>
